---
apiVersion: featuremanagement.cloudbees.io/v1alpha1
kind: flag-configuration

flag: TestingDisappearingConditions

enabled: false

stickinessProperty: rox.distinct_id

defaultValue:
  valueWrittenInCode: true

conditions:
  - allOf:
      - property:
          name: app_release
          operator: in-array
          operands:
            - newValue
    flagValue: false
  - allOf:
      - anyOf:
          - group:
              name: valid_names
          - group:
              name: valid_releases
    flagValue: false
